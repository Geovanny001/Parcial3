# Configuración de Base de Datos (MySQL en XAMPP)
DB_HOST=localhost
DB_USER=root
DB_PASS=  # Deja VACÍO para XAMPP por defecto (sin nada aquí, ni comillas)
DB_NAME=ecommerce

# Configuración de Stripe (para pagos – regístrate en stripe.com y usa clave de prueba)
STRIPE_KEY=sk_test_51ABC123def456ghi789jkl0mno  # Reemplaza con tu clave real de prueba

# Configuración de Redis (caché local)
REDIS_URL=redis://localhost:6379

# Puerto del servidor
PORT=3001